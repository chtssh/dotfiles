screen-list = playlist,artist
search-mode = 4
wrap-around = yes

# Display
welcome-screen-list = no
hide-cursor = 2
list-format = "%artist% - %title%"
status-format = "%artist% (%album%) - %track% %title% #[%date%#]"
#visible-bitrate = yes
set-xterm-title = yes

# Colors
enable-colors = yes
color title = blue
color list = white
color progressbar = green
color status-song = blue

# vim: ft=config
